name: Build pinned Steel image

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  build-pinned-image:
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact reviewed Steel source
        uses: actions/checkout@v4
        with:
          ref: bc65f35b1a8548d919520613bde6c8dd5cb4c63b

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push pinned linux/amd64 image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/daisuke134/steel-browser:sha-bc65f35b1a8548d919520613bde6c8dd5cb4c63b
          labels: |
            org.opencontainers.image.revision=bc65f35b1a8548d919520613bde6c8dd5cb4c63b
            org.opencontainers.image.source=https://github.com/Daisuke134/steel-browser
