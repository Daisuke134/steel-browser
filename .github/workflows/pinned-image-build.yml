name: Build pinned Steel image

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  build-pinned-image:
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact reviewed Steel source
        uses: actions/checkout@v4
        with:
          ref: 4b5cba6a8e56ee7481428385fbfa284b254f1abc

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push pinned linux/amd64 image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/daisuke134/steel-browser:sha-4b5cba6a8e56ee7481428385fbfa284b254f1abc
          labels: |
            org.opencontainers.image.revision=4b5cba6a8e56ee7481428385fbfa284b254f1abc
            org.opencontainers.image.source=https://github.com/Daisuke134/steel-browser
